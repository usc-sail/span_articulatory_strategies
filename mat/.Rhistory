setwd("~/spring2017/mat")
load("strategies_rep2.csv")
read.csv("strategies_rep2.csv")
tab1 <- read.csv("strategies_rep.csv")
tab2 <- read.csv("strategies_rep2.csv")
rbind(tab1,tab2)
tab2 <- read.csv("strategies_rep2.csv")
tab1 <- read.csv("strategies_rep.csv")
tab <- rbind(tab1,tab2)
nrow(tab)
nrow(tab1)
nrow(tab2)
tab1 <- read.csv("strategies_rep.csv")
tab1 <- subset(tab1,folder %in% c("eh3_rep","sg2_rep"))
tab2 <- read.csv("strategies_rep2.csv")
tab <- rbind(tab1,tab2)
nrow(ta)
nrow(tab)
aggregate(. ~ folder + task, tab, function(x) c(mean = mean(x), sd = sd(x)))
aggregate(. ~ task, tab, function(x) c(mean = mean(x), sd = sd(x)))
aggregate(. ~ folder + task, tab, function(x) c(mean = mean(x), sd = sd(x)))
tab2$folder <- apply(tab2$folder, function(x) substr(x,1,nchar(x)-1))
tab2$folder <- apply(tab2$folder, FUN = function(x) substr(x,1,nchar(x)-1))
help(apply)
tab2$folder <- dapply(tab2$folder, FUN = function(x) substr(x,1,nchar(x)-1))
tab2$folder <- lapply(tab2$folder, FUN = function(x) substr(x,1,nchar(x)-1))
tab2$folder <- lapply(tab2$folder, 1, FUN = function(x) substr(x,1,nchar(x)-1))
tab2$folder <- apply(tab2$folder, 1, FUN = function(x) substr(x,1,nchar(x)-1))
tab2$folder
substr(tab2$folder[1],1,nchar(tab2$folder[1])-1)
tab2$folder[1]
tab2 <- read.csv("strategies_rep2.csv",stringsAsFactors = FALSE)
tab2$folder <- apply(tab2$folder, 1, FUN = function(x) substr(x,1,nchar(x)-1))
substr(tab2$folder[1],1,nchar(tab2$folder[1])-1)
dim(tab2$folder)
tab2$folder <- apply(as.vector(tab2$folder), 1, FUN = function(x) substr(x,1,nchar(x)-1))
as.vector(tab2$folder)
dim(as.vector(tab2$folder))
tab2$folder <- apply(as.matrix(tab2$folder), 1, FUN = function(x) substr(x,1,nchar(x)-1))
tab2$folder
tab <- rbind(tab1,tab2)
tab
aggregate(. ~ folder + task, tab, function(x) c(mean = mean(x), sd = sd(x)))
View(aggregate(. ~ folder + task, tab, function(x) c(mean = mean(x), sd = sd(x))))
aggregate(. ~ folder + task, tab, function(x) c(mean = mean(x), sd = sd(x)))
tab$biomarker <- (tab$jawHann)/(tab$tngHann+tab$lipHann+tab$velHann+tab$jawHann)
tab$biomarker
hist(tab$biomarker)
range(tab$biomarker)
aggregate(biomarker ~ folder + task, tab, function(x) c(mean = mean(x), sd = sd(x)))
tab$biomarker <- tab$jawHann/tab$zHann
aggregate(biomarker ~ folder + task, tab, function(x) c(mean = mean(x), sd = sd(x)))
tab$jawbiomarker <- tab$jawHann/tab$zHann
tab$tngbiomarker <- tab$tngHann/tab$zHann
tab$lipbiomarker <- tab$lipHann/tab$zHann
aggregate(jawbiomarker ~ folder + task, tab, function(x) c(mean = mean(x), sd = sd(x)))
aggregate(tngbiomarker ~ folder + task, tab, function(x) c(mean = mean(x), sd = sd(x)))
aggregate(lipbiomarker ~ folder + task, tab, function(x) c(mean = mean(x), sd = sd(x)))
hist(tab@jawbiomarker)
hist(tab$jawbiomarker)
hist(tab$tngbiomarker)
hist(tab$lipbiomarker)
tab$zHann
plot(tab$zHann)
